#! /bin/bash

folder=$1
prefix=$1/usr
base=glpk-4.55
name=$base.tar.gz

[[ -f $prefix/lib/libglpk.so ]] && exit 0

mkdir -p $1 && cd $1
[[ -f $file ]] || wget ftp://ftp.gnu.org/gnu/glpk/$name
[[ -d $base ]] || tar -xzvf $name

cd $base
./configure
make
make prefix=$prefix install
